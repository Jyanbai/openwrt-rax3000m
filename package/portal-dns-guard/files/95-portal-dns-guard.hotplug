#!/bin/sh

[ "$INTERFACE" = "wan" ] || exit 0

case "$ACTION" in
	ifup|ifupdate|ifdown)
		# The watchdog owns all state transitions.  Hotplug only wakes it.
		ubus call service signal '{"name":"portal-dns-guard","signal":10}' >/dev/null 2>&1
		;;
esac

exit 0
